# Arquivo de configuração para criar o cluster com 1 control panel e 3 workers com o Kind
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4

nodes:
  - role: control-plane
  - role: worker
  - role: worker
  - role: worker  